startup.service: {
    type: oneshot,
    process_timeout: 360,
    command: "entrypoint.sh"
}

node.service: {
    command: "start.sh",
    after: "startup.service",
    restart: true
}

auto_update.service: {
    type: cron,
    after: "startup.service",
    interval: "0/5 * * * *",
    command: "gitpull.sh"
}

console.logging: {
    selector: '*.warn',
    stdout: true
}